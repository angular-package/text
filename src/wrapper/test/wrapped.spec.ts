import { Wrapped } from '../src/wrapped.class';
import { Wrapper } from '../src/wrapper.class';


typeof Wrapped;


// const wrapped = new Wrapped('My wrapped text');
// console.log(wrapped, wrapped.valueOf(), wrapped.unwrap());

// console.log(`1. `, new Wrapped('[bl[a', new Wrapper('[]')));
// console.log(`2. `, new Wrapped('[bl[a', '<>'), `<>`);
// console.log(`3. `, new Wrapped('<bl[a]>', '<>'), `<>`);
// console.log(`4. `, new Wrapped('<bl[a]>', new Wrapper('<>')), `new Wrapper('<>')`);
// console.log(`5. `, new Wrapped('<bl[a]>', new Wrapper(undefined)), `new Wrapper(undefined)`);
// console.log(`6. `, new Wrapped('[bl[a', '[]'), new Wrapped('[bl[a', '[]').unwrap());

